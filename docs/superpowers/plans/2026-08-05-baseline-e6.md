# Baseline E6 — Formalização da Configuração que Fecha o Envelope de CG — Plano

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** Adotar a configuração E6 (validada em campanha de experimentos) como novo baseline: trem principal avançado, bateria na cauda, bagageiro avançado, empenagem V_h 0,85 e profundor maior — TODAS mudanças de dados em `config/aircraft/baseline_4seat.toml`, mais as correções de consistência que os experimentos não cobriram (massa e arrasto da empenagem maior; corda do profundor coerente com a autoridade assumida).

**Architecture:** Só dados + pins + JSON. Nenhuma mudança de física.

## Global Constraints

- SI; português; pins honestos (old→new); nenhum requisito de missão alterado; `cargo test` verde; aircraft_spec.json regenerado/commitado.
- Resultado esperado: `validation_status: PASS` com zero violações (primeiro PASS honesto do projeto). Se qualquer margem virar violação com as correções de consistência (combustível, nariz ≥8%), NÃO mascarar: reportar NEEDS_CONTEXT com números.

---

### Task 1: Aplicar E6 + correções de consistência

**Files:**
- Modify: `config/aircraft/baseline_4seat.toml`, pins (tests/generic_engine.rs, tests/cli.rs, tests/acceptance.rs, tests/schema_v4.rs, tests/empennage.rs, tests/vn_diagram.rs, unit tests afetados), `aircraft_spec.json`.

**Mudanças no TOML (comentar cada uma com a motivação — campanha E1–E6 de 2026-08-05):**

1. `[gear] x_main_m = 3.85` → `3.55` — causa raiz do envelope vazio (limite de rotação 39,9%→22,2% MAC); carga de nariz cai p/ ~9%.
2. `[[masses.items]] avionicos: 60.0` → `32.0` + novo item `bateria_recolocada = 28.0` com `arm_ref = "empennage_cg"` — bateria no cone de cauda.
3. Item `cabos_hidraulico` ganha `arm_offset_m = 1.3` — rota de cabos até a bateria traseira.
4. `[arms] baggage_m = 5.60` → `4.90` — bagageiro avançado (comprime a dispersão de CG dos cenários com bagagem).
5. `[empennage] v_h = 0.70` → `0.85` — EH maior (S_h 2,58→3,13 m²): limite de rotação avança E ponto neutro recua (janela alarga dos dois lados).
6. `[control_surfaces] elevator_chord_frac = 0.35` → `0.40` — profundor maior, coerência geométrica com o item 7.
7. `[stability] cl_h_max_down = 0.85` → `0.95` — autoridade do profundor maior (corda 40% + deflexão típica); comentário DEVE citar a sensibilidade (parâmetro dominante; validar em ensaio) e a coerência com o item 6.
8. **Consistência (novo vs experimentos):** `[[masses.items]] emp_horizontal: 22.0` → `27.0` kg — escala pela área S_h (22·3,13/2,58 ≈ 26,7; arredondar 27; comentar a base). `[empennage] cd0 = 0.004` → `0.0046` — escala pela área molhada total da empenagem ((3,13+1,41)/(2,58+1,41) ≈ 1,138; comentar).

**Consequências esperadas (validar contra o run real; tolerâncias dos pins mantidas):**
- OEW 885 → ~890 kg (+5 da EH); CG vazio recua um pouco mais (bateria + EH maior na cauda).
- NP ≈ 3,50 m (44% MAC → limite traseiro ≈ 43,5% — recalcular com a EH de 27 kg... NB: massa não afeta NP, só CG); limite de rotação ≈ 10,9% MAC (form fechada, com os dados novos ~ igual à E6); flare pode dar % negativo (nunca governa — o print/schema já documentam).
- Envelope: TODOS os 6 cenários dentro → `PASS`, zero violações (aviso elétrico permanece).
- Arrasto: cd0 total sobe ~0,00006·1.04 → V_max cai ~0,5–1 km/h; consumo/missão sobem um fio; margem de combustível ~4,5–5% (POSITIVA — se negativar, NEEDS_CONTEXT).
- Carga de nariz ~9% (≥8% — se cair abaixo, NEEDS_CONTEXT).
- MTOW missão re-converge (+5 kg OEW + arrasto): ~1538 kg.

**Steps (TDD invertido — golden update honesto):**
- [ ] Rodar o baseline ATUAL e registrar os valores-chave (tabela old no relatório).
- [ ] Aplicar as 8 mudanças no TOML com comentários.
- [ ] `cargo run`: verificar PASS/zero violações e as consequências acima; investigar qualquer surpresa >1% antes de prosseguir.
- [ ] Atualizar TODOS os pins quebrados honestamente (old→new comentado; tolerâncias iguais); testes que asseravam o estado FAIL/envelope-vazio do baseline mudam de sentido: renomear/reescrever para asserar o PASS (mantendo um teste sintético que preserva a detecção de envelope-vazio via config mutada em código — o caminho de erro continua coberto).
- [ ] Suite completa; commit `feat(config): baseline E6 — envelope de CG fechado (trem, bateria, bagageiro, empenagem, profundor)`; aircraft_spec.json commitado.
