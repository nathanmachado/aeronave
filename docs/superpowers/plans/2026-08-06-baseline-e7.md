# Baseline E7 — Tipback fechado e premissa de autonomia 7 h — Plano

**Goal:** Adotar a configuração E7 (validada na campanha de 2026-08-06, 24+24 experimentos por TOML) como novo baseline: premissa de missão reduzida para 7 h + reserva, e trem principal recuado 11 cm para fechar o tipback. Primeiro PASS com os 18 checks do ciclo 2 ativos.

**Architecture:** Só dados + pins + JSON. Nenhuma mudança de física.

## Global Constraints

- SI; português; pins honestos (old→new, tolerâncias iguais); `cargo test` verde; aircraft_spec.json regenerado/commitado.
- Resultado esperado: `validation_status: PASS`, zero violações (aviso elétrico permanece). Se qualquer check reprovar no run real, NÃO mascarar: reportar com números.

---

### Task 1: Aplicar E7

**Files:** Modify: `config/missions/default.toml`, `config/aircraft/baseline_4seat.toml`, pins (tests/generic_engine.rs, tests/cli.rs, tests/acceptance.rs, tests/gear_tipback.rs, tests/schema_v4.rs, unit tests afetados), `aircraft_spec.json`.

**Mudanças (comentar cada uma com a motivação — campanha E7 de 2026-08-06):**

1. `[missão] endurance_min_h = 8.0` → `7.0` — decisão de requisito do cliente (2026-08-06): autonomia 7 h de voo + reserva (reserva de voo de 10% e gate de 5% da capacidade inalterados). Atualizar o comentário do bloco `min_fuel_margin_fraction` que descreve a violação de 1,58% — com 7 h a margem vira ~14% e o gate passa.
2. `[gear] x_main_m = 3.55` → `3.66` — fecha o tipback (10,08°→15,58° ≥ 15°) mantendo carga de nariz ≤25% (24,8%) e envelope 6/6; só é viável porque a autoridade DATCOM (ciclo 2) alargou o limite de rotação para 13,0% MAC nesta posição. Janela viável da campanha: 3,65–3,66 m; 3,66 escolhido pela melhor pior-margem. Atualizar o comentário longo do bloco [gear] que documenta a violação de tipback.

**Consequências esperadas (validar contra o run real; pins honestos):**
- Combustível exigido 255,9 → ~223,7 L; margem 1,58% → ~13,97%; endurance 7,60 → ~7,68 h; range ~2.149 km; v_cruise ~301,9 km/h; MTOW ~1.548,4 kg (envelope governa; missão re-converge mais leve).
- Trem: tipback ~15,58°, tail-strike ~14,88°, nariz ~13,0–24,8%; limite de rotação ~13,0% MAC.
- `validation_status: FAIL` → `PASS`, violações 2 → 0. Testes que asseravam o FAIL honesto (tipback/margem — tests/cli.rs, tests/gear_tipback.rs, tests/generic_engine.rs) mudam de sentido: reescrever para asserar o PASS, preservando a detecção dos caminhos de erro via configs sintéticas mutadas em código (cobertura dos checks #15 e #18 continua).

**Steps (golden update honesto, padrão E6):**
- [ ] Rodar o baseline ATUAL e registrar valores-chave (tabela old no relatório).
- [ ] Aplicar as 2 mudanças com comentários.
- [ ] `cargo run`: verificar PASS/zero violações e consequências; investigar surpresa >1% antes de prosseguir.
- [ ] Atualizar TODOS os pins quebrados (old→new comentado; tolerâncias iguais).
- [ ] Suite completa; commit `feat(config): baseline E7 — tipback fechado e autonomia 7h+reserva (trem 3,66 m)`; aircraft_spec.json commitado.
