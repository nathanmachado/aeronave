[package]
name = "aeronave"
version = "0.1.0"
edition = "2021"
description = "Modelagem matemática de aeronave experimental 4 lugares — Toyota 1GD-FTV"

[dependencies]
clap = { version = "4.6.5", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
