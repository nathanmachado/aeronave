# Baseline E10 — Adoção da Configuração Robusta (Híbrido + Trem Curto + Flap Slotted) — Plano

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adotar a célula E10 recomendada (campanha de 2026-08-08, pós-ciclo-7, 6 células PASS-robusto) como baseline: primeiro PASS completo do projeto com os 24 checks + robustez de 3 mundos.

**Architecture:** Só dados + pins + JSON + desenho. Nenhuma mudança de física. Decisão do usuário (2026-08-08): "sim adote E10 como baseline".

**Tech Stack:** Rust (binário existente), python (gera_desenho.py).

## Global Constraints

- SI; Português; pins honestos (old→new comentado, tolerâncias INALTERADAS); `cargo test` verde; `aircraft_spec.json` regenerado/commitado; desenho regenerado.
- Resultado esperado: `validation_status: PASS`, ZERO violações, ZERO flips (aviso elétrico pré-existente permanece). Se QUALQUER check reprovar no run real, NÃO mascarar: reportar com números e PARAR.

## Valores E10 (todos verificados na campanha `campanha_e10c.py`, célula clf2.1/cmf-0.30/tff0.35)

| Campo | De → Para | Motivação (comentar no TOML citando campanha E10 2026-08-08) |
|---|---|---|
| `[[masses.items]] bateria_recolocada` | 28,0 → **53,0 kg** + `arm_offset_m = 0.4` (CG 7,80 m) | compensação de peso por BATERIA (diretriz híbrido): fecha envelope/nariz robustos; ~3 kWh úteis |
| `[gear] x_nose_m` | 1,40 → **1,30** | carga de nariz robusta (teto 25%) |
| `[gear] h_cg_ground_m` | 1,05 → **0,92** | tipback robusto com bateria pesada; folga de hélice re-checada com D menor |
| `[mass_model] main_strut_length_m` | 0,67 → **0,54** | coerência das pernas com h_cg −13 cm |
| `[mass_model] nose_strut_length_m` | 0,53 → **0,40** | idem |
| `[propeller] diameter_m` | 1,95 → **1,76** | folga de solo 0,24 m ≥ 0,23 com trem curto; Mach de ponta folgado |
| `[wing] cl_max_flaps` | 1,72 → **2,1** | flap SLOTTED (decisão construtiva do usuário): pouso grama 556,7 m < 600 |
| `[wing] cm_flap_delta` | −0,30 (inalterado — conferir comentário) | slotted moderado; célula recomendada usa −0,30 |
| `[stability] to_flap_fraction` | 0,5 → **0,35** | decolagem com flap parcial: rotação robusta (+21,6 pp pós-perturbação) |

Consequências esperadas (validar contra o run real; investigar surpresa >1%): PASS/0 violações/0 flips; OEW ~+13,8 kg; MTOW ~+13,8; margem combustível ~9,14%; autonomia ~7,23 h; cruzeiro ~300,2 km/h; pouso grama ~556,7; decolagem grama ~?<600; nariz ~22,77%; folga hélice 0,240.

---

### Task 1: Aplicar E10 + golden update honesto + JSON

**Files:** Modify: `config/aircraft/baseline_4seat.toml`, pins quebrados em `src/` e `tests/` (cli.rs muda de sentido: FAIL 4 violações + 2 flips → **PASS 0/0** — reescrever asserando o PASS e preservando a cobertura dos caminhos de erro via configs sintéticas já existentes), `aircraft_spec.json` (regenerado).

- [ ] Rodar o baseline ATUAL e registrar valores-chave (tabela old no report).
- [ ] Aplicar as mudanças com comentários (campanha E10, 2026-08-08; cada linha com valor antigo).
- [ ] `cargo run --release` (comando padrão, --out /tmp primeiro): verificar PASS/0/0 e consequências; PARAR em surpresa.
- [ ] Golden update honesto de TODOS os pins; regenerar `aircraft_spec.json` para o repo.
- [ ] Suite completa; commit `feat(config): baseline E10 — primeiro PASS robusto completo (híbrido 53kg, trem curto, hélice 1,76, flap slotted)`.

### Task 2: Desenho + relatório

**Files:** Modify: `docs/aeronave_e7_rascunho.svg` → regenerado (o script lê o TOML/JSON novos; se o nome do arquivo citar E7, renomear para `docs/aeronave_e10_rascunho.svg` atualizando referências), report no workspace.

- [ ] `python3 docs/gera_desenho.py` (conferir uso/saída no próprio script); commit do desenho.
- [ ] Report: tabela old→new completa (massas, CG, checks, desempenho) + o marco: primeiro PASS robusto.
- [ ] Suite completa verde; commit `docs: desenho E10 regenerado`.
